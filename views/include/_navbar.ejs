<nav class="navbar navbar-expand-lg navbar-dark  bg-dark ">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">108SHOP</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link" id="shop" href="/shop">Shop</a>
          </li>
          <% if(currentUser && currentUser.role === "Admin") {%>
            <li class="nav-item">
                <a class="nav-link" id="newProduct" href="/shop/new">New Product</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" id="orderManagement" href="/orders/order-management">Manage Order</a>
              </li>
          <% } %> 
        </ul>
        <ul class="navbar-nav mr-auto mb-2 mb-lg-0">
          <% if(currentUser && currentUser.role === "User") {%>
            <li class="nav-item">
                <a class="nav-link " aria-current="page" href="/cart"><i class="fas fa-shopping-cart"></i> Cart
                <% if(currentUser) { %> 
                    (<%= currentUser.cart.items.length %>) 
                <% } %> 
                </a>
            </li>
            <% } %> 
            <% if(!currentUser) { %> 
                <li class="nav-item">
                    <a class="nav-link" id="login" aria-current="page" href="/login">Login</a>
                  </li>
                  <li class="nav-item ">
                    <a class="nav-link " id="register" aria-current="page"  href="/register">Register</a>
                  </li>
            <% }else{ %> 
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="fas fa-user"></i>
                <%= currentUser.username%>
                <% if(currentUser.role === "Admin") {%>
                    (Admin)
                <% } %> 
              </a>
              <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <% if(currentUser && currentUser.role === "User") {%>
                <li><a class="dropdown-item" href="/orders">Orders</a></li>
                <li><hr class="dropdown-divider"></li>
                <% } %> 
                <li><a class="dropdown-item" href="/logout">Logout</a></li>
              </ul>
            </li>
            <% } %> 
          </ul>
     
      </div>
    </div>
  </nav>